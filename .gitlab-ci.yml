---
include:
  - https://gitlab.com/ngine/ci/-/raw/main/docker-gitlab-template-multi.yml

container-build-gitlab-push:
  extends: .container-build-push
  before_script:
    - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY
    - export $(grep -v '^#' ./VERSION | xargs -0)
